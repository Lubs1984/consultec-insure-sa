# ─── Railway Environment Variables ──────────────────────────────────────────
#
# Copy these into each Railway service's "Variables" tab.
# PostgreSQL + Redis values are auto-injected by Railway plugins —
# just add the plugin and the variable appears automatically.
#
# ────────────────────────────────────────────────────────────────────────────

# ── API Service ──────────────────────────────────────────────────────────────

NODE_ENV=production
API_PORT=3001

# Automatically set by Railway PostgreSQL plugin:
# DATABASE_URL=postgresql://...
# DIRECT_URL=postgresql://...   ← set same value as DATABASE_URL on Railway

# Automatically set by Railway Redis plugin:
# REDIS_URL=redis://...

# Generate with: openssl rand -hex 32
JWT_ACCESS_SECRET=REPLACE_WITH_RANDOM_64_CHAR_HEX
JWT_REFRESH_SECRET=REPLACE_WITH_DIFFERENT_RANDOM_64_CHAR_HEX

JWT_ACCESS_EXPIRY=15m
JWT_REFRESH_EXPIRY=7d

# Your Railway API service public URL (set after first deploy)
API_URL=https://YOUR_API_RAILWAY_URL.up.railway.app

# Your Railway web service public URL (or Vercel URL)
WEB_URL=https://YOUR_WEB_URL

APP_DOMAIN=insureconsultec.co.za

# ── Optional (add later) ─────────────────────────────────────────────────────

# Cloudflare R2 (document storage)
# R2_ACCOUNT_ID=
# R2_ACCESS_KEY_ID=
# R2_SECRET_ACCESS_KEY=
# R2_BUCKET_NAME=insureconsultec-documents
# R2_PUBLIC_URL=

# SendGrid (email)
# SENDGRID_API_KEY=SG.xxx
# SENDGRID_FROM_EMAIL=noreply@insureconsultec.co.za

# Africa's Talking (SMS)
# AT_API_KEY=
# AT_USERNAME=insureconsultec
